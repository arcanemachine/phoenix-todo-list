---
services:
  phoenix:
    image: "arcanemachine/phoenix-todo-list:latest"
    container_name: "phoenix-todo-list--phoenix"
    restart: "always"
    environment:
      # phoenix
      PHX_HOST: "${PHX_HOST:?}"
      PORT: "${PORT:?}"
      SECRET_KEY_BASE: "${SECRET_KEY_BASE:?}"

      # db
      DATABASE_URL: "${DATABASE_URL:?}"
      # POSTGRES_DB: "${POSTGRES_DB:?}"
      # POSTGRES_USER: "${POSTGRES_USER:?}"
      # POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?}"
