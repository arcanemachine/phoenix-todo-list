---
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: todo_list
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    network_mode: host
    volumes:
      - "./support/docker/volumes/postgres:/var/lib/postgresql/data"
  phoenix:
    depends_on:
      - db
