---
services:
  phoenix:
    environment:
      DATABASE_URL: "${DATABASE_URL:?}"
    networks:
      - "traefik-global-proxy"
      - "phoenix-todo-list"
    ports:
      - "${PORT:?}"

networks:
  traefik-global-proxy:
    external: true
  phoenix-todo-list:
