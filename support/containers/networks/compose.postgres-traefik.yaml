---
services:
  postgres:
    hostname: "postgres"
    networks:
      - "phoenix-todo-list"
    ports:
      - "5432"
#   # depends_on:
#   #   - "traefik"
#   phoenix:
#     environment:
#       POSTGRES_HOST: "postgres"

networks:
  traefik-global-proxy:
    external: true
  phoenix-todo-list:
